# For running `npm run dev` locally (outside Docker)
# Copy to .env.local and fill in values

# The proxy target (your Unraid API)
VITE_API_TARGET=http://192.168.178.40:9200

# Bearer token (same as API_SECRET_KEY in your .env)
VITE_API_TOKEN=
